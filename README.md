# tracking

Super awesome 3D pose tracking with the tensorflow, mediapipe model in unity.

The code essentially takes the keypoints in the pose and finds the joint rotations which are applied to the 3d model. Very fun and epic
